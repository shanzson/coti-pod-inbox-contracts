# Verification of external findings #17–#32 (all claimed Low)

Scope A = `/home/user/coti-pod-inbox-contracts` (HEAD = 89121ad + one comment). Scope B = `/home/user/coti-contracts` (= 8a0c492). "Prior #n" = our validated report `audit/privacy-portal-pashov-ai-audit-report-20260905.md`. Nothing was compiled or run; the only tooling used was a read-only Python scan of the existing `artifacts/build-info` files (see #28).

---

### #17 — Shipped fee templates use the Hardhat-only 1/1 gas-price skew; documented safeguards absent (claimed: Low)
- Verdict: PARTIALLY CONFIRMED (config/doc facts confirmed; the "silent busy return leg" impact analysis is wrong).
- Evidence:
  - `scripts/deploy-utils.ts:382-383` `gasPriceMul: 1n, gasPriceDiv: 1n,` (FEE_CONFIG_SEPOLIA_SIDE) and `:401-402` same (FEE_CONFIG_COTI_SIDE) — line refs accurate. `:426-427` `gasPriceMul: BigInt(j.gasPriceMul ?? 1), gasPriceDiv: BigInt(j.gasPriceDiv ?? 1),` — accurate (silent default). `:447-460` `testnetMinFeeConfigsForChain` hands these templates to Sepolia/Fuji/COTI-testnet deploys (used by `scripts/deploy-full-testnet.ts:142`).
  - `docs/ESTIMATE_EXECUTION_GAS.md:61` "Default `1/1` is **Hardhat-only**; live L1↔COTI lanes must ship a measured non-identity remote ratio"; `:82` "PEI deploy refuses remote `mul == div` … unless `allowGasPriceSkewOneToOne`"; `:89` `node scripts/measure-gas-price-skew.mjs`; `:98` "`LIVE_LANE_REMOTE_GAS_PRICE_SKEW` in `scripts/deploy-utils.ts`"; table `:104-109` (5/1, 13/1, 1/5, 1/10). A grep of both repos (excluding node_modules/artifacts) finds `LIVE_LANE_REMOTE_GAS_PRICE_SKEW`, `allowGasPriceSkewOneToOne`, `measure-gas-price-skew` only in that doc — absent from code, as claimed. Note the doc itself places the guard and the script in the out-of-scope PEI repo (`:47`, `:82`, `:86`); only the constant is claimed for this repo.
  - Skew application: `contracts/fee/FeeManager.sol:181` `_applyGasPriceSkew(Math.mulDiv(remoteGasWei / gasPrice, localPrice, remotePrice), remoteMin)`; `:178` `callerGasLocalUnits = _applyGasPriceSkew(callbackFeeLocalWei / gasPrice, localMin)`.
  - No event on config change: `FeeManager.sol:130-139` `updateMinFeeConfigs` writes `$.localMinFeeConfig`/`$.remoteMinFeeConfig` with no `emit` — the "not observable without polling" claim is accurate.
- What it gets wrong: the claim that the identity skew silently under-recovers "the return leg — the direction carrying every withdrawal callback and every response" is incorrect. Reply legs never re-price gas on COTI: `InboxBase.sol:279` passes `incomingRequest.callerFee` as the return-leg budget (`IInbox.sol:157` "Gas for the return leg is **not** charged again: it uses the `callerFee` budget"), and that budget was fixed on the *source* chain by the source's *local* template (`FeeManager.sol:178`, `localMin`), where 1/1 is the correct local→local value. COTI's *remote* template only prices sends originated on COTI; no in-scope contract originates sends from COTI (`PodErc20CotiMother` only `respond`/`raise`, `:461/:482/:531/:557/:574`). The self-announcing inbound overpayment half is right. Fix is sound (fail-loud loader, deploy guard, constants, `FeeConfigUpdated` event); `updateMinFeeConfigs` (`InboxMiner.sol:243`) can correct a live lane, as the finding notes.
- Severity: agree Low (deployment-config hygiene; owner-correctable; no exploit). Would accept Informational.
- Gist PoC: none.
- Overlap: none.

### #18 — Adopting an existing pToken does not check it corresponds to the paired collateral (claimed: Low)
- Verdict: CONFIRMED.
- Evidence: `PrivacyPortalFactory.sol:607-611` `createPortalWithExistingPToken(...) external onlyRole(DEFAULT_ADMIN_ROLE)` (line 611 accurate). Ownership gate `:616-619` `if (pTokenOwner != address(this)) revert PTokenNotOwnedByFactory`. Same-factory guards `:625-637` (`UnderlyingPTokenMismatch`, `PTokenAlreadyPaired`) are skipped when all three mappings are empty; the only remaining checks are `:639-646` decimals (`DecimalsMismatch`) and `:647-649` native flag. `:651-652` `address oldPortal = existingPortalForPToken; if (oldPortal != address(0))` — retire/pause guards nested under a local mapping, so a foreign old portal is untouched; `:674` `setMinter(portal)` moves minting. Old portal keeps paying: `PrivacyPortal.sol:853-876` `_releaseWithdrawal` needs only `pToken.requests(...).status == Success` (`:861-864`), no minter. pToken has no collateral notion: `grep -ri underlying contracts/pod/token/perc20/` → no hits. Gist `F6_PortalExitPaths.t.sol:110-130` does `factory.transferPTokenOwnership(pt, factory2)` then `factory2.createPortalWithExistingPToken(usdt, pt, false)` and asserts the USDT binding.
- Overstatement: the example direction is inverted — with "pDAI+WETH" one mints against the *greater* (WETH) and redeems the *lesser* (DAI); the draining direction is pWETH paired with DAI. Class of issue unaffected. Fix (derive previous portal from `minter()` and assert `underlyingToken()`) is sound and operator-input-free.
- Severity: agree Low (two admins' actions on two factories; realistic error is same-value stablecoins).
- Gist PoC: `F6_PortalExitPaths.t.sol` (`test_F6_adoptBindsMismatchedUnderlying`).
- Overlap: prior #4 (cross-factory remount skips old-portal guards; our fix requires the previous minter paused **and** retired, which removes the "two live portals" exploit, but does **not** assert underlying equality — that residual from this finding is still open and worth adding to `_requirePreviousMinterClosed`).

### #19 — Portal accepts deposits before its pToken is registered on COTI; nonce ordering prevents loss (claimed: Low)
- Verdict: CONFIRMED (as a liveness/documentation residual; the loss claim is correctly withdrawn).
- Evidence: `PrivacyPortalFactory.sol:543-549` `createPortal … onlyDeployer`; `:584-586` `initialize(...)`; `:592` registration; `:744-745` `sendOneWayMessage{value: msg.value}(cotiChainId, cotiMotherContract, methodCall, bytes4(0))` (one-way, no error selector). `PrivacyPortal.sol:255` `isDepositEnabled = true;` and no pause in `initialize` (line accurate). NatSpec `:531-542` (finding's 532). Shared nonce stream: `InboxBase.sol:507` `uint256 nonce = ++_requestNonce[targetChainId];` (accurate); COTI ingest order: `InboxMiner.sol:55-58, 70` `if (minedNonce != allowedNonce) revert NoncesNotContiguous();`. Residue (a): `PodErc20CotiMother.sol:102-111` `onlyRegisteredFactoryMessage` → `FactoryNotAllowed`; retry is permissionless `InboxMiner.sol:294`. Residue (b): `PrivacyPortal.sol:592` `if (mintStatus != IPodERC20.RequestStatus.SystemFailed) revert DepositMintNotFailed` — a never-ingested mint stays `Pending`; only `adminRefundPendingDeposit` (`:614-619`, `whenPaused`) recovers. `configurePToken` `PrivacyPortalFactory.sol:375` → `PodERC20.sol:156-161` `setInbox` (accurate).
- Overstatement/misses: (c) is overstated — the NatSpec says "leave **(or immediately set)** {PrivacyPortal.isDepositEnabled} to `false`" (`:538`), so it does contemplate the second tx; the real gap is role split (`createPortal` is `onlyDeployer` `:549`, `setIsDepositEnabled` is `onlyFactoryOperator` `PrivacyPortal.sol:281`). Missed residue: if registration's first mine fails and is not retried within `maxMessageLife` (`FeeManager.sol:22` 48h), `retryFailedRequest` terminalizes it (`InboxMiner.sol:307-312`), the one-way message has `errorSelector == 0` so nothing is signalled (`InboxBase.sol:570-572`), `_requestMotherRegistration` is `private` with its only caller at `:592`, and `createPortal` refuses the same underlying (`:553-555`) — the pToken can then never be registered through this factory. Fix (create paused; correct NatSpec; TTL-gated permissionless refund) is sound; our prior #5 fix already creates the clone paused.
- Severity: agree Low.
- Gist PoC: `NonceContiguityReal.t.sol`, `F1_PreRegistrationDepositWindow.t.sol`.
- Overlap: prior #5 (direct), prior #8 (residue b: `Failed` not refundable), prior lead "One-shot, unacknowledged mother registration".

### #20 — Two-way request fields validated when sent but not when received (claimed: Low)
- Verdict: CONFIRMED (one sub-claim wrong).
- Evidence: `InboxMiner.sol:104-119` copies `callbackSelector: minedRequest.callbackSelector, errorSelector: minedRequest.errorSelector, isTwoWay: minedRequest.isTwoWay, … callerFee: minedRequest.callerFee` with no validation (also `_ingestMinerReject` `:182-197`). Send-side: `InboxBase.sol:194-196` `if (callbackSelector == bytes4(0) || errorSelector == bytes4(0) || callbackSelector == errorSelector) revert InvalidTwoWaySelectors();`; `FeeManager.sol:166-168` `if (callbackFeeLocalWei == 0) revert CallbackFeeTooLow`, `:183-185` min check — refs accurate. Recovery reads the same fields: `InboxBase.sol:570-572` `if (incomingRequest.errorSelector == bytes4(0)) { return; }`, `:585-590` `if (incomingRequest.callerFee == 0) { … emit SystemErrorRaised … return; }` — accurate. Reply guards: `:256-258` `ZeroCallbackBudget` / `NoErrorHandler` / `NoCallbackHandler`; `:260` `replySelector = isRaise ? errorSelector : callbackSelector` — equal selectors deliver a raise to the success selector.
- What it gets wrong: "on the pToken path `transferCallback` records Success" — no. `PodERC20.sol:336` sets `Success` but `:337-345` then `abi.decode(data, (address, ctUint256, ctUint256, address, ctUint256, ctUint256, uint256))` (11 words); the mother's raise payload is `abi.encode(from, to, reason)` (`PodErc20CotiMother.sol:574`, ≤6 words), so the decode reverts, the callback reverts as a whole and the source Inbox records `EXECUTION_FAILED` (`InboxMiner.sol:385-391`) — fail-closed there. The fail-open case does exist on `approveCallback` (`PodERC20.sol:380-390`: 6-word decode; `abi.encode(tokenOwner, spender, reason)` at mother `:579` with a ≥1-word reason is ≥6 words and word 3 = offset `0x60` decodes as a valid address), which would write garbage allowance ciphertext and `Success`. Fix (re-assert the triple at ingest, incl. the reject path) is sound.
- Severity: agree Low (requires a malformed `MinedRequest` from a trusted miner; miner authority is already an acknowledged High).
- Gist PoC: `A2_TwoWayTripleNotCrossValidated.t.sol` (5 tests; equal-selector case untested per README).
- Overlap: none.

### #21 — The two delegate-call modules cannot be replaced without redeploying the Inbox (claimed: Low)
- Verdict: CONFIRMED (design observation, not a defect).
- Evidence: `InboxBase.sol:176` `mpcAbiReEncode = _mpcAbiReEncode;` and `:178` `feeManager = _feeManager;` are the only writes (`grep -rn "feeManager\s*=" contracts/` → `:177-178` only; `mpcAbiReEncode\s*=` → `:176` only); `_initInboxBase` guarded by `:173` `if (_initialized) revert AlreadyInitialized();`. `MpcAbiReEncode.sol` is 262 lines (accurate). Intent: `Inbox.sol:15-16` "this contract *is* the live instance (no separate implementation)".
- Overstatement: "some of it immutable (PodERC20 clones fix cotiChainId at construction)" — the *Inbox address* is rotatable everywhere: `PodERC20.sol:156-166` `configure(inbox_, cotiSideContract_)` → `setInbox`; `PodErc20CotiMother.sol:161-167` `configure(inbox_)`; `PrivacyPortalFactory.sol:361-372` `configureRouting`. Only `cotiChainId` is fixed (`PodERC20.sol:152`), which is not the Inbox address. A redeploy costs the deterministic-address property and strands in-flight requests (prior #14), not immutability. Fix (timelocked two-step rotation) is sound in principle; note `FeeManager` storage is ERC-7201 on the Inbox (`FeeManager.sol:10`, `LibFeeStorage.sol:219-233`), so any replacement must preserve that layout.
- Severity: propose Informational — documented design choice with no vulnerability or likelihood; it is an incident-response trade-off.
- Gist PoC: none.
- Overlap: prior #14 (request-id reuse across inbox rotation) tangentially.

### #22 — Dynamic fee floor/cap in wei while the fee is USD-denominated (claimed: Low)
- Verdict: CONFIRMED.
- Evidence: `PrivacyPortalFeeLib.sol:68-75` `calculateDynamicFee`: `fee = percentageFeeNative > fixedFee ? percentageFeeNative : fixedFee; return fee > maxFee ? maxFee : fee;` (finding's 69 ≈ accurate); `:79-97` `resolvePortalFee`; `:92-94` `txValueUsd`, `percentageFeeUsd`, `percentageFeeNative = Math.mulDiv(percentageFeeUsd, PRICE_SCALE, nativeUsdRate)` (line 94 accurate); `:95-96` returns `(fee, true)` whether or not clamped. `:32` `if (maxFee == 0 || fixedFee > maxFee || …) revert InvalidFeeConfiguration();` (accurate). Units: `PrivacyPortalFactory.sol:168-173` "fee floor in native wei … fee cap in native wei". Charge path drops the flag: `PrivacyPortal.sol:917` `(floor,) = PrivacyPortalFeeLib.resolvePortalFee(...)`.
- Assessment: arithmetic correct, unit mismatch real; "protocol loss" is bounded by the operator-chosen cap and retunable via `setDefaultDepositFee`/`setDepositFee` (`PrivacyPortalFactory.sol:453`, `PrivacyPortal.sol:356`). Fix (USD-denominated bounds or a `clamped` signal) is sound; the "allow maxFee unset" option conflicts with the deliberate `maxFee != 0` guard.
- Severity: agree Low (revenue/UX drift, no user fund risk; Informational also defensible).
- Gist PoC: `F7_PortalFeeLibBounds.prop.t.sol` (`test_F7_dynamicPricingSilentlyClamps`).
- Overlap: prior #11 (fail-open / `usedDynamicPricing` monitoring); prior #7 tangential.

### #23 — Remount safety sequence enforced on the path, not the capability (claimed: Low)
- Verdict: CONFIRMED.
- Evidence: remount sequence `PrivacyPortalFactory.sol:651-678` (`:653` `if (!IPrivacyPortal(oldPortal).paused()) revert OldPortalNotPaused`, `:665` `retireDepositsForUpgrade()`, `:668-673` clone + `pauseByFactory()`, `:674` `setMinter(portal)`, `:676-678` mappings) — accurate. Raw primitives `:385-391` `transferPTokenOwnership` → `Ownable(pToken_).transferOwnership(newOwner_)`, `:394-397` `setPTokenMinter` → `setMinter(newMinter_)`, both guarded only by `:412-420` `_requireFactoryOwnedPToken` + zero check — accurate. `PodErc20Mintable.sol:96-103` `setMinter … onlyOwner` (accurate). Bricking: `PrivacyPortal.sol:419` `pToken.mint{value: mintFee}` → `PodERC20.sol:270` `_checkMinter()` → `PodErc20Mintable.sol:47-51` `OnlyMinter`; `PodERC20.sol:573-574` `invalidatePendingRequest … _checkMinter()` (accurate) called from `PrivacyPortal.sol:632-633`. No mapping/pause update on either primitive; after ownership transfer `_requireFactoryOwnedPToken` (`:416-419`) fails on the old factory (accurate).
- Overstatement: "both mutations are demonstrated in the accompanying PortalExitPaths test material" — `F6_PortalExitPaths.t.sol` exercises only `transferPTokenOwnership` (`:117`); no gist test calls `setPTokenMinter` (grep across F6/P17). Fix (parity with remount path, keep mappings in sync, events) is sound; our prior #4 fix already adds the paused-portal requirement to `setPTokenMinter` (`test/portal-poc/contracts/patched/PrivacyPortalFactoryFixed.sol:403-411`); `transferPTokenOwnership` remains unguarded there (`:394-400`).
- Severity: agree Low (good-faith admin misuse; recoverable by rotating back).
- Gist PoC: `F6_PortalExitPaths.t.sol` (ownership transfer only), `P17_RemountMinterRotation.t.sol` (remount path).
- Overlap: prior #3, prior #4 (fix covers `setPTokenMinter`), prior lead "`setPTokenMinter` leaves the mapped portal advertising itself live".

### #24 — `PodErc20CotiMother` ownership can be renounced, freezing allow-listing and inbox rotation (claimed: Low)
- Verdict: CONFIRMED.
- Evidence: `PodErc20CotiMother.sol:6` `import "@openzeppelin/contracts/access/Ownable.sol";`, `:14` `contract PodErc20CotiMother is IPodErc20CotiSide, InboxUser, Ownable`; `grep -rn renounceOwnership contracts/pod` → only `PodERC20.sol:148` — no override on the mother. `:152` `function setAllowedFactory(...) external onlyOwner` and `:161` `function configure(address inbox_) external onlyOwner` (both accurate); `InboxUser.sol:73` `setInbox` is `internal`, so no other route. Comparators: `Inbox.sol:40-42` `renounceOwnership() public pure override { revert(); }`; `PodERC20.sol:148-150`; `PrivacyPortalFactory.sol:263-266` `if (_adminCount <= 1) revert CannotRevokeLastAdmin();`. Oracles all plain `Ownable` with no override: `PriceOracle.sol:10`, `PoDPriceOracle.sol:10`, `chainlink/ChainlinkLiveOracle.sol:11`, `band/BandLiveOracle.sol:11`, `PortalFeeOracle.sol:129` (grep shows no `renounceOwnership` in Scope A `contracts/fee`).
- Nothing overstated. Fix (override to revert) is sound and matches the codebase's own pattern.
- Severity: agree Low.
- Gist PoC: none.
- Overlap: none.

### #25 — A batch burn whose acknowledgement is lost can never be resolved (claimed: Low)
- Verdict: PARTIALLY CONFIRMED (permanent `pendingBurnAmount` inflation is real; "never resolvable / no reconciliation" is overstated).
- Evidence: `PrivacyPortal.sol:703-724` `finalizeBatchBurn` (finding's 698 is the NatSpec line): `Success` → `:711-713` clears reservation and `pendingBurnAmount -= amount`; `Failed`/`SystemFailed` → `:718-719` clears reservation only; else `:722` `revert BatchBurnNotResolved`. `:59` `pendingBurnAmount`, `:672-696` `burnAccumulatedPTokens`, `:682` `available = pendingBurnAmount - burnInFlightTotal` — accurate. Mother commits before acking: `PodErc20CotiMother.sol:453` `_writeGarbledBalance(id, from, senderAfter);` then `:461` `inbox.respond(` — accurate. Lost ack leaves the source request `Pending` (`PodERC20.sol:1069` `_setRequestStatus(requestId, Pending)`; only `transferCallback:336`, `transferError:437`, `_handleSystemError:686`, `killStaleRequest:602`, `invalidatePendingRequest:578` move it). `pendingBurnAmount` written only at `:866` and `:713` — accurate; no setter.
- What it overstates: the reservation *is* admin-resolvable. `PrivacyPortalFactory.sol:406-409` `killPTokenStaleRequest` → `PodERC20.sol:590-605` (`:597` age gate, default `requestKillMinAge = 1 days` `:67`, `:602` sets `Failed`), after which `finalizeBatchBurn` takes the `Failed` branch and clears `burnInFlight`/`burnInFlightTotal`. So `burnInFlight*` are not "permanently stuck" and "no setter or reconciliation" is only true for `pendingBurnAmount`. The residual is exactly that: `pendingBurnAmount` stays inflated by pTokens already burned on COTI, and after the kill `available` re-admits a phantom resubmission that would `raise` on COTI (`PodErc20CotiMother.sol:425-433` insufficient balance) — wasted fees, no loss. The recommended "admin function releasing an aged reservation" already exists in effect (the kill); what is missing is a `pendingBurnAmount` reconciliation and the "remote outcome unknown" status.
- Severity: agree Low.
- Gist PoC: `SystemFailedReachability.t.sol` (Q2/Q3; neither exercises the kill route for burns); `P13_PortalBurnLedger.t.sol` is a retired banner.
- Overlap: prior leads "`pendingBurnAmount` can be permanently over-stated" and "No on-chain record of pTokens actually held on COTI" (unscored observations).

### #26 — A configuration change can strand paid in-flight messages; one message fails a batch (claimed: Low)
- Verdict: PARTIALLY CONFIRMED (batch atomicity, no grandfathering and the three cap copies confirmed; sub-claim (ii) is inaccurate).
- Evidence: `InboxMiner.sol:93-102` `if (weight > maxMethodCallBytes) revert MethodCallTooLarge…; if (minedRequest.targetFee > maxLocalExecutionGas) revert FeeGasTooHigh…; if (minedRequest.callerFee > maxRemoteExecutionGas) revert FeeGasTooHigh…` inside the single loop (`:60-159`) — one revert fails the whole batch (accurate). Caps read at ingest `:48-52`; `:243` `updateMinFeeConfigs … onlyOwner` immediate (accurate). `:70` `if (minedNonce != allowedNonce) revert NoncesNotContiguous();` and reject item path `:80-91` (accurate). (i) `FeeManager.sol:92-99` `localRequestExecutionBudget`: `totalFee > errorBuffer ? totalFee - errorBuffer : 0` — accurate. (iii) `InboxBase.sol:620-626` `_requireReplyMethodCallBounded` vs `:494` `weight > remoteMax.maxMethodCallBytes` vs `InboxMiner.sol:94`; `FeeManager.sol:143` `if (maxBytes == 0) revert` vs `:264` `PROTOCOL_MAX_METHOD_CALL_BYTES` (32,768) — accurate. Cursor write `:161-163` `lastIncomingRequestId[sourceChainId] = mined[mined.length - 1].requestId;` — accurate.
- What it gets wrong: (ii) `retryFailedRequest` (`:294-315`) and `_runIncomingExecution` (`:323-396`) re-check no admission cap, so lowering *local* `maxExecutionGas` cannot make a retry revert `FeeGasTooHigh`. The only `FeeGasTooHigh` reachable on retry is inside a reply leg: `respond`/`raise` → `_sendOneWayMessage(... incomingRequest.callerFee ...)` (`InboxBase.sol:273-282`) → `_createRequest:497` `if (targetFeeGas > remoteMax.maxExecutionGas) revert FeeGasTooHigh` when the *remote* cap was lowered below the stored `callerFee`. In that case the TTL system-error callback (`:601-610` → same `_createRequest:497`) reverts too, so it does not "survive the change" — `retryFailedRequest:309-310` sets `EXPIRED` and sends the callback in one tx, so the request cannot be terminalized until the cap is raised (a sharper variant than stated). Mitigation not mentioned: `estimateExecutionGasForMiner` applies the identical caps (`InboxEstimateGas.sol:105-114`), so a miner can pre-screen a blocker before batching. Fix: "commit the valid prefix" is workable (the loop already stops at the first revert; only the cursor semantics change) but leaves the blocker in place until rejected; grandfathering/routing over-cap items into the error path is the complete fix.
- Severity: agree Low (needs an owner action; recovery exists via the reject lane).
- Gist PoC: none.
- Overlap: none.

### #27 — Build not reproducible; dependency diverged from its published form (claimed: Low)
- Verdict: PARTIALLY CONFIRMED (local facts confirmed with one inaccuracy; registry claims unverifiable).
- Evidence: `package.json:48` `"@coti-io/coti-contracts": "file:../coti-contracts",` (accurate). `package-lock.json:110-113` `"node_modules/@coti-io/coti-contracts": { "resolved": "../coti-contracts", "link": true }` — no `integrity` (accurate); but `package-lock.json:29-31` `"../coti-contracts": { "name": "@coti-io/coti-contracts", "version": "0.0.1", …` — a version **is** recorded (the sibling's local one), so "records no version" is inaccurate; it records no commit. `node_modules/@coti-io/coti-contracts -> ../../../coti-contracts` symlink; `/home/user/coti-contracts/package.json:3` `"version": "0.0.1"` (accurate). CI `.github/workflows/ci.yml:37` `ref: ${{ github.event.inputs.contracts_ref || 'main' }}` (accurate). MpcCore inlined: `MpcAbiReEncode.sol:4` imports `MpcCore.sol`, `:206` `MpcCore.validateCiphertext(itValue)` (internal library → inlined). Divergence half: commit `26e1503` exists in the local coti-contracts object store ("Fix transferWithAllowance allowance MPC type tag to SUINT32_T (M-32)", 1 file) and HEAD `MpcCore.sol:918` tags the gtUint32 allowance path `SUINT32_T` (accurate). `.github/workflows/npm-publish.yml` exists at HEAD (tag-triggered OIDC publish, `:1-40`).
- Unverifiable here: npm registry contents (0.1.0–1.3.5), what 1.3.5 tags, and "the npm-publish workflow was reverted" (local clone has a single commit; no history to inspect). Fix (pin to commit/integrity, pin CI ref, record revision per deployment, republish) is sound.
- Severity: agree Low (provenance/supply-chain; no in-scope contract consumes the npm package).
- Gist PoC: none.
- Overlap: none.

### #28 — Audited compiler configuration cannot produce a COTI-deployable artifact (claimed: Low)
- Verdict: PARTIALLY CONFIRMED (config facts confirmed and corroborated by Scope B's own comments; node behaviour, PUSH0 counts and byte sizes unverifiable here).
- Evidence: `hardhat.config.ts:92-94` `// shanghai (PUSH0): needed for Inbox ≤24_576 …` / `// Target chains (Sepolia / Fuji / COTI) support Shanghai …` / `evmVersion: "shanghai",` (accurate); single compiler block, no per-network profile (`grep -n evmVersion hardhat.config.ts` → only `:94`); `hardhat.config.chain1.ts:5-6` / `chain2.ts:5-6` set no `evmVersion` and are local-only (31337/31338). COTI is a deploy target: `hardhat.config.ts:129-134` `cotiTestnet … chainId: 7082400`, `scripts/deploy-full-testnet.ts:22, 162-181` deploys the Inbox and `MpcExecutor` on `COTI_NETWORK`. Scope B contradicts the config comment: `/home/user/coti-contracts/hardhat.config.ts:30, 49` `evmVersion: "paris"`; `PodERC20.sol:18` "so the whole tree can compile for Paris — COTI rejects Shanghai `PUSH0`"; `PrivacyPortalFactory.sol:27` "so bytecode stays Paris-compatible for COTI"; `MpcAbiReEncode.sol:234` "(Shanghai — no `mcopy`)". `scripts/check-bytecode-size.mjs:1-60` checks size only, no opcode preflight (accurate).
- Unverifiable: the RPC observation (`0x5f` → "invalid opcode: PUSH0"), the per-contract PUSH0 counts, the deploy failure, and "Paris runtime is 24,485 bytes". The only compiled output present locally (`artifacts/build-info/*.output.json`, settings `evmVersion: shanghai`) covers the test PoC sources, not the production `Inbox`, so a local opcode scan could not confirm the counts. Fix (Paris for COTI artifacts, correct the comment, opcode preflight, size gate) is sound; note the config's own claim that Shanghai was needed to fit under 24,576 (`:92`) means a Paris build must be size-checked.
- Severity: agree Low (deployment path defect; no deployed-contract risk). If COTI-side contracts are live, the provenance question (which config produced them) is the substantive point and is unverifiable from the repo.
- Gist PoC: none (live observation).
- Overlap: none.

### #29 — The gas-price skew ratio has no upper bound (claimed: Low)
- Verdict: CONFIRMED.
- Evidence: `FeeManager.sol:259-284` `_requireValidFeeConfig`: `:260-262` zero `maxMethodCallBytes`/`maxExecutionGas`, `:263-268` protocol caps, `:269-271` `if (feeConfig.gasPriceMul == 0 || feeConfig.gasPriceDiv == 0) revert FeeConfigInvalid` (finding's 270 accurate), `:272-283` constant/variable field checks — no bound on `mul/div`. Types `FeeManagerStubBase.sol:24-25` `uint16 gasPriceMul; uint16 gasPriceDiv;`. Application `FeeManager.sol:286-292` `Math.mulDiv(gasUnits, mul, div)`; inverse (ceil) in the quote `FeeManagerStubBase.sol:178, 181`.
- Accurate as stated ("validation gap, not an arithmetic defect"). One evidentiary gap: `Target2_FeeQuote.t.sol` (`test_skew_worstCaseUnderpricing`, "calls updateMinFeeConfigs on the real Inbox with mul=65535") is not in the gist (README:136 references it as external); `F10_FeeRoundTrip.t.sol` is pure arithmetic and its own comment `:75` says "NO 65535/1 case is added here". Fix (symmetric sanity cap) is sound; both orientations are legitimate per `docs/ESTIMATE_EXECUTION_GAS.md:104-109` (13/1 and 1/10), so the bound must allow at least ~1/20..20/1.
- Severity: agree Low (owner-only misconfiguration; Informational also defensible).
- Gist PoC: `F10_FeeRoundTrip.t.sol`.
- Overlap: none.

### #30 — Fee-admin authorisation lives in an override; one contract is deployable without it (claimed: Low)
- Verdict: PARTIALLY CONFIRMED (structural observation valid; both concrete instances are wrong in substance).
- Evidence: `FeeManagerStubBase.sol:188` `setPriceOracle`, `:192` `setGasPriceBounds`, `:201` `updateMinFeeConfigs`, `:210` `setMaxReplyMethodCallBytes`, `:214` `setMaxMessageLife`, `:218` `collectFees` — **six** `public virtual` forwarders with no modifier (the finding counts five, omitting `setGasPriceBounds`); design note `:13` "subclasses override admin entrypoints with `onlyOwner`". `InboxMiner.sol:224, 232-238, 243, 248, 254, 284` override all six with `onlyOwner` (accurate for the five cited). (a) `FeeManager.sol:80-88` `collectFees` unguarded, `:82` `address(this).balance`; payable entry points `:92, :160, :195` (accurate). (b) `InboxBase.sol:12` `contract InboxBase is IInbox, FeeManagerStubBase` — non-abstract (accurate).
- What it gets wrong: (b) a bare `InboxBase` is inert, not exploitable: `feeManager` is written only in `_initInboxBase` (`InboxBase.sol:172-180`, `internal`; `grep "feeManager\s*="` → `:178` only), whose sole external caller is `Inbox.init` (`Inbox.sol:35`). With `feeManager == address(0)` every forwarder reverts at `ModuleCallBase.sol:17` `if (module == address(0)) revert ModuleNotConfigured(module);` before any delegatecall, so no one can sweep, repoint the oracle or rewrite configs on a deployed `InboxBase`. (a) "stray value … unrecoverable" contradicts the finding's own observation: the implementation's `collectFees` is callable by anyone, so stray value is anyone-can-take, not stuck. Fix (mark `InboxBase` abstract; `_authorizeFeeAdmin` hook that reverts by default; non-owner negative test) is sound hygiene.
- Severity: propose Informational — no reachable exposure in the tree; purely structural.
- Gist PoC: none.
- Overlap: none.

### #31 — The initialisation path does not enforce its own preconditions; the deployment script does (claimed: Low)
- Verdict: CONFIRMED (both gaps); the primary recommended fix needs re-checking.
- Evidence: Gap 1 — `Inbox.sol:20` `constructor() Ownable(address(1)) {}`, `:28-31` `init(...) external initializer` with no `msg.sender` check, `:14` "Split deploy-then-initialize is unsafe; use CreateX `deployCreate3AndInit`" (all accurate); `_delegateModule` runs `feeManager` in Inbox storage (`ModuleCallBase.sol:18`). Gap 2 — `InboxBase.sol:177` `if (_feeManager == address(0)) revert ModuleNotConfigured(_feeManager);` (zero check only; accurate); `ModuleCallBase.sol:16-23` `_delegateModule`: `:17` zero check, `:18` `module.delegatecall(callData)`, no code check (accurate). A codeless target returns success with empty returndata, so `_ensureFeeDefaults` (`FeeManagerStubBase.sol:251-253`, return discarded) passes at init; the six admin forwarders (`:188-220`) silently no-op; `abi.decode(result, …)` at `:232, :242, :248` reverts on empty data (accurate); write-once (`InboxBase.sol:178`) so unrecoverable (accurate). `mpcAbiReEncode` codeless fails closed as an encode error (`InboxBase.sol:704-716`, `_tryDecodeAbiBytes` rejects `retLen == 0`). Scripts: `scripts/createx.ts:248-283` single `deployCreate3AndInit` tx with `init` calldata (`:255-264`); `:67-87` `buildInboxSalt` = `deployer(20) || 0x00 || entropy(11)`; `scripts/deploy-utils.ts:863-875` reads `owner()` and throws on `address(1)` or `owner != deployer`; `:839-846` deploys `FeeManager` itself via CREATE3 (all accurate).
- Fix caveat: "record the deployer in the constructor and require init's caller to equal it (under CreateX that is the CreateX contract)" — `init` is called by the CreateX contract (`createx.ts:23-60` ABI, `:271` `deployCreate3AndInit`), but under CREATE3 the contract is created by an intermediate proxy, so the constructor's `msg.sender` is not necessarily the CreateX contract; this cannot be verified from the repos (CreateX source not vendored) and must be checked before adoption — a safer form is `require(msg.sender == CREATEX)` in `init` (`createx.ts:20` `CREATEX_ADDRESS = 0xba5Ed…ba5Ed`), which confines `init` to the atomic path. Gap-2 fix (`code.length == 0 → revert`) is sound and already the repo's pattern elsewhere (`PrivacyPortalFactory.sol:334-336, 351-353, 474-476`).
- Severity: agree Low as shipped (scripts close both; gap 1 becomes Critical only on an off-script deploy).
- Gist PoC: none.
- Overlap: none.

### #32 — A confidential input cannot be produced by the user; the amount must be disclosed to whoever signs it (claimed: Low)
- Verdict: NOT VERIFIABLE FROM CODE for the core claim (precompile requires signer == `tx.origin`); the code path it rests on is CONFIRMED.
- Evidence: `PodERC20.sol:174, 179` `transfer(address, itUint256 …)`, `:185, 190` `transferFrom`, `:200` `transferAndCall`, `:253, 258` `approve`, `:264` `burn`, `:269` `mint` (all accurate); none inspects `value` locally. Validation happens on COTI under the miner's tx: `InboxMiner.sol:34-37` `batchProcessRequests … onlyMiner`, → `InboxBase.sol:704-706` `target.delegatecall(abi.encodeWithSelector(MpcAbiReEncode.reEncodeWithGt.selector, methodCall))` → `MpcAbiReEncode.sol:203-210`, `:206` `gtUint256 gtValue = MpcCore.validateCiphertext(itValue);` (accurate) → `MpcCore.sol:1301-1304` forwards `(SUINT256_T, ctHigh, ctLow, input.signature)` to `ExtendedOperations(0x64).ValidateCiphertext` (`MpcInterface.sol:42-43, 48`). `tx.origin` is therefore the miner EOA whenever validation runs (delegatecall preserves it). The `itUint256` struct carries only `ciphertext` + `signature` (`MpcCore.sol:102-105`) — no account field (accurate). Portal uses public forms: `PrivacyPortal.sol:419, 467` `pToken.mint{value: mintFee}(recipient, received, mintCallbackFee)` (accurate), `:692` `pToken.burn{value: msg.value}(amount, burnCallbackFee)` (finding says 693 — off by one).
- Unverifiable: that the precompile binds the signature to `tx.origin` (no such rule is documented in either repo; the `ValidateCiphertext` ABI takes no signer argument, which is consistent with but does not prove it). The finding's evidence is three live testnet transactions. If true, the privacy-model consequence follows directly from the code path above. Documentation half of the fix is sound; the "bind the originating account into what is signed" half requires precompile changes outside both repos.
- Severity: agree Low as a privacy-model/documentation issue (conditional on the precompile behaviour); no fund risk.
- Gist PoC: none (README maps nothing; `MockMpcPrecompile.sol` models logic, "not cryptography").
- Overlap: none.

---

Summary: CONFIRMED — #18, #19, #20, #21, #22, #23, #24, #29, #31. PARTIALLY CONFIRMED — #17, #25, #26, #27, #28, #30. NOT VERIFIABLE (core claim) — #32. Severity changes proposed: #21 → Informational, #30 → Informational; all others agree Low. Notable corrections: #17's "busy return leg" narrative (reply legs use the source-computed `callerFee`), #20's `transferCallback` fail-open sub-claim (actually `approveCallback`), #25's "never resolvable" (kill → `Failed` clears the reservation; only `pendingBurnAmount` stays inflated), #26 (ii) (no cap re-check on retry; remote-cap lowering also breaks the TTL callback), #30 (b) (a bare `InboxBase` is inert), and #31's proposed constructor-sender check under CREATE3.
